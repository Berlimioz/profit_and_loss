= simple_form_for @cost_entry do |f|
  - if @cost_entry.errors.any?
    #error_explanation
      %h2 #{pluralize(@cost_entry.errors.count, "erreur") } ont empêché cette entrée d'être enregistrée:
      %ul
        - @cost_entry.errors.full_messages.each do |message|
          %li= message
  = f.input :name
  = f.input :ttc_price
  = f.input :information
  = f.button :submit
