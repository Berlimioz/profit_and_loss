<%= form_for(@profit_entry) do |f| %>
  <% if @profit_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profit_entry.errors.count, "error") %> prohibited this profit_entry from being saved:</h2>

      <ul>
      <% @profit_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ttc_price_in_cents %><br>
    <%= f.number_field :ttc_price_in_cents %>
  </div>
  <div class="field">
    <%= f.label :information %><br>
    <%= f.text_area :information %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
